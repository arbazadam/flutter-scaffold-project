import 'package:flutter/material.dart';

class AlarmsScreen  extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Welcome to the Alarm screen")
    );
  }
}